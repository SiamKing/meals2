<%= form_for(user_meal) do |f| %>
  <% if user_meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_meal.errors.count, "error") %> prohibited this user_meal from being saved:</h2>

      <ul>
      <% user_meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
